import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";

// Read-only static SPA. Data bundles live in public/data and are generated by
// scripts/build-data.mjs from ../data/world/**/*.json before the dev server runs.
export default defineConfig({
  plugins: [react()],
  server: { port: 5174, open: false },
});
